# Socratic-OT runtime requirements
# Pinned versions for Cloud Run reproducibility. Update via `pip freeze | grep -i ...`
# and re-test before bumping.

# ── LLM / Anthropic ──────────────────────────────────────────────────────────
anthropic[bedrock,vertex]==0.86.0
boto3==1.42.96
# openai — only needed if vlm_node is implemented; commented out until then.

# ── Orchestration ────────────────────────────────────────────────────────────
langchain-core==1.2.21
langgraph==1.1.3
langgraph-checkpoint==4.0.1
langgraph-checkpoint-sqlite==3.0.3
langgraph-checkpoint-postgres==2.0.21
psycopg[binary,pool]==3.2.3
langgraph-prebuilt==1.0.8
langgraph-sdk==0.3.12

# ── Retrieval ────────────────────────────────────────────────────────────────
chromadb==1.5.5
nltk==3.9.4
ollama==0.6.1                    # optional dev backend; not required on Cloud Run
sentence-transformers==5.3.0     # cross-encoder reranker
tokenizers==0.22.2
torch==2.10.0
transformers==5.3.0              # nomic-embed query embeddings
einops==0.8.0                    # required by nomic-bert-2048 modeling file

# ── PDF ingestion (dev-only — ChromaDB is baked into the Cloud Run image) ───
# Uncomment if rerunning ingest/run_ingest_pipeline.py:
# PyMuPDF==1.26.4

# ── API server ───────────────────────────────────────────────────────────────
fastapi==0.136.1
httpx==0.28.1
pydantic==2.12.5
pydantic-settings==2.13.1
starlette==1.0.0
uvicorn[standard]==0.42.0

# ── Persistence ──────────────────────────────────────────────────────────────
aiosqlite==0.22.1
sqlite-vec==0.1.9

# ── Utilities ────────────────────────────────────────────────────────────────
pillow==12.2.0
python-dotenv==1.2.2
tqdm==4.67.3

# ── Cross-session memory (optional; activated by MEMORY_BACKEND=mem0) ───────
# When MEMORY_BACKEND != "mem0" the client falls back to a no-op so the
# package is unused at runtime — installing it costs only image size.
mem0ai==0.1.106
